Гайд по структуре проекта

1️⃣ tsconfig.json — главный, “root”-конфиг

Это основной файл TypeScript, который управляет всеми остальными.
Обычно он не содержит реальных настроек, а просто ссылается на другие:

{
  "files": [],
  "references": [
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.app.json" }
  ]
}

💡 Зачем он нужен:

Используется для сборки проекта целиком (через tsc --build).
Делит проект на два контекста — “Node” (сборка и конфиг) и “App” (фронтенд-код).
Улучшает скорость проверки и типизацию при больших проектах (TypeScript умеет кэшировать отдельные конфиги).
Можно считать это “мастер-файлом”, который объединяет всё.

2️⃣ tsconfig.node.json — для окружения Node.js

Этот файл отвечает за всё, что не браузер, т.е. за файлы конфигурации (например vite.config.ts, postcss.config.ts, tailwind.config.ts).

{
  "compilerOptions": {
    "composite": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "allowSyntheticDefaultImports": true,
    "types": ["node"]
  },
  "include": ["vite.config.ts", "tailwind.config.ts"]
}

💡 Что тут важно:

"types": ["node"] — позволяет использовать типы Node.js (process, __dirname и т.д.).
"include" указывает файлы, где эти типы нужны.
"composite": true — говорит TypeScript, что этот конфиг можно использовать как “подпроект” (для tsc --build).
📎 Другими словами — этот файл нужен, чтобы TypeScript понимал твой конфиг-код, который выполняется в Node.

3️⃣ tsconfig.app.json — для клиентского кода (React / Vite)

Это основной TS-конфиг твоего приложения, в котором ты реально работаешь.
Пример (типичный для Vite + React + TS):

{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "moduleResolution": "Bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    // 👉 вот тут можно добавить алиасы
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@app/*": ["src/app/*"],
      "@features/*": ["src/features/*"],
      "@shared/*": ["src/shared/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}

💡 Что тут важно:

"noEmit": true — TypeScript не компилирует JS, потому что это делает Vite.
"lib" включает доступ к DOM, ES2020, Iterable — без этого TS не знает про браузерные API.
"jsx": "react-jsx" — новый JSX-трансформ, поддерживаемый React 17+.
"strict": true — строгая типизация (рекомендовано включать).
"paths" — ключевая штука: алиасы для импорта (@/shared/...).
📎 Этот файл фактически определяет “поведение TS внутри приложения”.

👉 Когда ты запускаешь:

vite dev → используется tsconfig.app.json (через Vite)
tsc --build → использует tsconfig.json, который подтягивает оба
vite.config.ts → использует tsconfig.node.json